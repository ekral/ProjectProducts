<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ProjectProducts.NewProductPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand">
            <Entry Text="{Binding NewProduct.Name}" Keyboard="Text" Placeholder="Name"/>
            <Entry Text="{Binding NewProduct.Price}" Keyboard="Numeric" Placeholder="Price"/>
            <Button Text="Save" Command="{Binding SaveCommand}" />
            <Button Text="Dismiss" Command="{Binding DismissCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>